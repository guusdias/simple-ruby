<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Loja" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <%= nav_bar brand: "Ruby Store", brand_link: root_url do%>
      <%= menu_group do%>
          <%= menu_item "Criar novo produto", new_product_path%>
          <%= menu_item "Departamentos", departaments_path%>
      <%end%>
      <div class="form-group">
        <%= form_tag search_product_path, method: :get, class: "navbar-form navbar-right" do%>
          <%= text_field_tag :name, @name, class:"form-control"%>
          <%= button_to "Buscar", nil, class:"btn btn-default"%>
        <%end%>
      </div>
      <%end%>
      <div class="container">
        <%= yield %>
      </div>
  </body>
</html>
